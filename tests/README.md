# tests

Cross-cutting and integration test suites that span multiple apps or packages.

- Unit tests live alongside their source (`*.test.ts`) and run via **Vitest**.
- App-level e2e tests live under each app (`apps/*/tests/e2e`) and run via **Playwright**.
- Place broader integration / contract tests here.

This directory is a scaffold placeholder.
