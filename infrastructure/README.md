# infrastructure

Infrastructure-as-Code and deployment assets for AGE.

Intended contents (to be added as the platform matures):

- `docker/` — production Dockerfiles and compose overrides
- `terraform/` or `pulumi/` — cloud provisioning
- `k8s/` — Kubernetes manifests / Helm charts
- `env/` — environment-specific configuration

This directory is a scaffold placeholder. No infrastructure is defined yet.
