{
  "name": "@age/capture",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "age-capture": "./dist/main.js"
  },
  "exports": {
    ".": "./src/index.ts",
    "./composition": "./src/capture-composition.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@age/business-discovery-capture": "workspace:*",
    "@age/business-discovery-contracts": "workspace:*",
    "@age/capability-kit": "workspace:*",
    "@age/scored-bif-snapshot-persistence": "workspace:*",
    "@prisma/client": "^5.20.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "vitest": "^2.1.1"
  }
}
