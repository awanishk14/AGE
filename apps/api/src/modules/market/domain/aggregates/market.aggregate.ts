import { AggregateRoot } from '@age/shared';

/**
 * MarketAggregate — aggregate root (consistency boundary) for the market context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class MarketAggregate extends AggregateRoot {}
