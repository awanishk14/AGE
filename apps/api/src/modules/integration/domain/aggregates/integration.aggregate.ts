import { AggregateRoot } from '@age/shared';

/**
 * IntegrationAggregate — aggregate root (consistency boundary) for the integration context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class IntegrationAggregate extends AggregateRoot {}
