import { AggregateRoot } from '@age/shared';

/**
 * PeopleAggregate — aggregate root (consistency boundary) for the people context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class PeopleAggregate extends AggregateRoot {}
