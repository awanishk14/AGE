import { Injectable } from '@nestjs/common';

/**
 * IcpService — application service (use-cases) for the icp domain.
 * Placeholder; no business logic yet.
 */
@Injectable()
export class IcpService {
  /** Placeholder status indicator for the scaffolded icp module. */
  status(): string {
    return 'icp module: scaffold only';
  }
}
