import { AggregateRoot } from '@age/shared';

/**
 * IcpAggregate — aggregate root (consistency boundary) for the icp context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class IcpAggregate extends AggregateRoot {}
