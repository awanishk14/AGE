import { AggregateRoot } from '@age/shared';

/**
 * KnowledgeAggregate — aggregate root (consistency boundary) for the knowledge context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class KnowledgeAggregate extends AggregateRoot {}
