import { AggregateRoot } from '@age/shared';

/**
 * DecisionAggregate — aggregate root (consistency boundary) for the decision context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class DecisionAggregate extends AggregateRoot {}
