import { AggregateRoot } from '@age/shared';

/**
 * ResearchAggregate — aggregate root (consistency boundary) for the research context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class ResearchAggregate extends AggregateRoot {}
