import { AggregateRoot } from '@age/shared';

/**
 * ProblemAggregate — aggregate root (consistency boundary) for the problem context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class ProblemAggregate extends AggregateRoot {}
