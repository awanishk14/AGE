import { AggregateRoot } from '@age/shared';

/**
 * OrganizationAggregate — aggregate root (consistency boundary) for the organization context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class OrganizationAggregate extends AggregateRoot {}
