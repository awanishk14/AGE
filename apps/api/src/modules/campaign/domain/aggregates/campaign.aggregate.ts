import { AggregateRoot } from '@age/shared';

/**
 * CampaignAggregate — aggregate root (consistency boundary) for the campaign context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class CampaignAggregate extends AggregateRoot {}
