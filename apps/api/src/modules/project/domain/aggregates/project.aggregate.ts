import { AggregateRoot } from '@age/shared';

/**
 * ProjectAggregate — aggregate root (consistency boundary) for the project context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class ProjectAggregate extends AggregateRoot {}
