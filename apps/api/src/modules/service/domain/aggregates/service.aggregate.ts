import { AggregateRoot } from '@age/shared';

/**
 * ServiceAggregate — aggregate root (consistency boundary) for the service context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class ServiceAggregate extends AggregateRoot {}
