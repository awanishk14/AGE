import { Injectable } from '@nestjs/common';

/**
 * StrategyService — application service (use-cases) for the strategy domain.
 * Placeholder; no business logic yet.
 */
@Injectable()
export class StrategyService {
  /** Placeholder status indicator for the scaffolded strategy module. */
  status(): string {
    return 'strategy module: scaffold only';
  }
}
