import { AggregateRoot } from '@age/shared';

/**
 * StrategyAggregate — aggregate root (consistency boundary) for the strategy context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class StrategyAggregate extends AggregateRoot {}
