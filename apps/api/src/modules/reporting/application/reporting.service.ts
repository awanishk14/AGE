import { Injectable } from '@nestjs/common';

/**
 * ReportingService — application service (use-cases) for the reporting domain.
 * Placeholder; no business logic yet.
 */
@Injectable()
export class ReportingService {
  /** Placeholder status indicator for the scaffolded reporting module. */
  status(): string {
    return 'reporting module: scaffold only';
  }
}
