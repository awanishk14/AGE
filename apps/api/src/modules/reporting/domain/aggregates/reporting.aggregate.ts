import { AggregateRoot } from '@age/shared';

/**
 * ReportingAggregate — aggregate root (consistency boundary) for the reporting context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class ReportingAggregate extends AggregateRoot {}
