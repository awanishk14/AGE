import { AggregateRoot } from '@age/shared';

/**
 * WorkflowAggregate — aggregate root (consistency boundary) for the workflow context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class WorkflowAggregate extends AggregateRoot {}
