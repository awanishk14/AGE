import { AggregateRoot } from '@age/shared';

/**
 * BrandAggregate — aggregate root (consistency boundary) for the brand context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class BrandAggregate extends AggregateRoot {}
