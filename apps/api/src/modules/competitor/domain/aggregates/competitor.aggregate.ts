import { AggregateRoot } from '@age/shared';

/**
 * CompetitorAggregate — aggregate root (consistency boundary) for the competitor context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class CompetitorAggregate extends AggregateRoot {}
