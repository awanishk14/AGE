import { Injectable } from '@nestjs/common';

/**
 * CompetitorService — application service (use-cases) for the competitor domain.
 * Placeholder; no business logic yet.
 */
@Injectable()
export class CompetitorService {
  /** Placeholder status indicator for the scaffolded competitor module. */
  status(): string {
    return 'competitor module: scaffold only';
  }
}
