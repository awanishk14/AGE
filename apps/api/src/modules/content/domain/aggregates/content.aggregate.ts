import { AggregateRoot } from '@age/shared';

/**
 * ContentAggregate — aggregate root (consistency boundary) for the content context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class ContentAggregate extends AggregateRoot {}
