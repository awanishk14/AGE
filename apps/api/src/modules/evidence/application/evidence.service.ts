import { Injectable } from '@nestjs/common';

/**
 * EvidenceService — application service (use-cases) for the evidence domain.
 * Placeholder; no business logic yet.
 */
@Injectable()
export class EvidenceService {
  /** Placeholder status indicator for the scaffolded evidence module. */
  status(): string {
    return 'evidence module: scaffold only';
  }
}
