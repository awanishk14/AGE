import { AggregateRoot } from '@age/shared';

/**
 * EvidenceAggregate — aggregate root (consistency boundary) for the evidence context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class EvidenceAggregate extends AggregateRoot {}
