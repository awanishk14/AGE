import { AggregateRoot } from '@age/shared';

/**
 * ProductAggregate — aggregate root (consistency boundary) for the product context.
 * Canonical domain root. Placeholder; invariants and child entities added later.
 */
export class ProductAggregate extends AggregateRoot {}
