/**
 * @age/types — shared types and Zod schemas for the AGE platform.
 *
 * Scaffold only. Domain models are intentionally not implemented yet.
 */

export const AGE_TYPES_PACKAGE = '@age/types' as const;
