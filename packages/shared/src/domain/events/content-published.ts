import { DomainEvent } from '../domain-event';

/** ContentPublished domain event. Placeholder; payload added during implementation. */
export class ContentPublished extends DomainEvent {
  readonly eventName = 'content.published';
}
