import { DomainEvent } from '../domain-event';

/** ProjectCompleted domain event. Placeholder; payload added during implementation. */
export class ProjectCompleted extends DomainEvent {
  readonly eventName = 'project.completed';
}
