import { DomainEvent } from '../domain-event';

/** EvidenceAdded domain event. Placeholder; payload added during implementation. */
export class EvidenceAdded extends DomainEvent {
  readonly eventName = 'evidence.added';
}
