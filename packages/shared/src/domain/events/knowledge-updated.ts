import { DomainEvent } from '../domain-event';

/** KnowledgeUpdated domain event. Placeholder; payload added during implementation. */
export class KnowledgeUpdated extends DomainEvent {
  readonly eventName = 'knowledge.updated';
}
