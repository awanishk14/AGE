import { DomainEvent } from '../domain-event';

/** DecisionApproved domain event. Placeholder; payload added during implementation. */
export class DecisionApproved extends DomainEvent {
  readonly eventName = 'decision.approved';
}
