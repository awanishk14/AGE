import { DomainEvent } from '../domain-event';

/** CampaignApproved domain event. Placeholder; payload added during implementation. */
export class CampaignApproved extends DomainEvent {
  readonly eventName = 'campaign.approved';
}
