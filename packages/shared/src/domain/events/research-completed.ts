import { DomainEvent } from '../domain-event';

/** ResearchCompleted domain event. Placeholder; payload added during implementation. */
export class ResearchCompleted extends DomainEvent {
  readonly eventName = 'research.completed';
}
