/**
 * EvidenceModel — Represents evidence and its provenance within the knowledge layer.
 *
 * Placeholder interface. No implementation.
 */
export interface EvidenceModel {
  readonly name: 'EvidenceModel';
}
