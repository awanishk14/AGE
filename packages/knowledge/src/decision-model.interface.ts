/**
 * DecisionModel — Represents decisions and links them to supporting evidence.
 *
 * Placeholder interface. No implementation.
 */
export interface DecisionModel {
  readonly name: 'DecisionModel';
}
