{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "sdk",
  "projectType": "library",
  "sourceRoot": "packages/sdk/src",
  "tags": ["scope:shared", "type:sdk"],
  "targets": {
    "build": { "command": "tsc -p tsconfig.json", "options": { "cwd": "packages/sdk" } },
    "typecheck": {
      "command": "tsc -p tsconfig.json --noEmit",
      "options": { "cwd": "packages/sdk" }
    },
    "lint": { "command": "eslint .", "options": { "cwd": "packages/sdk" } },
    "test": { "command": "vitest run --passWithNoTests", "options": { "cwd": "packages/sdk" } }
  }
}
